[build]
  publish = "."
  command = "echo 'Static site - no build needed'"

# Skip secrets scanning - these are intentionally public client-side API keys
# Firebase and Google Calendar keys are restricted by domain/security rules
[secrets-scanning]
  skip = true
