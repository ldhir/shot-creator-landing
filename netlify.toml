[build]
  publish = "."
  command = "echo 'Static site - no build needed'"

[build.environment]
  # Disable smart detection for client-side API keys (Firebase/Google Calendar)
  SECRETS_SCAN_SMART_DETECTION_ENABLED = "false"
