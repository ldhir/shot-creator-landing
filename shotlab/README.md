# ShotLab (branch-only)

Run the ShotLab backend from this branch with:

```
python tool/shotlab_app.py
```

Then open:

```
http://localhost:5001/shotlab
```

Notes:
- This file is intentionally separate from tool/app.py so ShotLab changes remain isolated to this branch.
